/* ================= 全局统一配置 Global_CFG =================
   卷积超参数 + 3D 场景布局都改这里；第 1–5 节的场景尺寸、动画计数、
   UI 文案数字（HTML 中的 <span data-v="...">）全部由它推导，改后全页生效。
   默认：输入 1×3×32×32 → Conv → 输出 1×32×16×16（输出面边长按卷积公式
   ⌊(N+2p−k)/s⌋+1 = ⌊(32+2×1−3)/2⌋+1 = 16 计算所得，不写死）。 */
const Global_CFG = {
  inN: 16,       // 3D 演示输入面边长 N×N
  inC: 3,        // 输入深度 = R/G/B 通道
  pad: 1,        // pads 1,1,1,1：上下左右各扩 1 格
  stride: 2,     // 滑窗步长
  kSize: 3,      // 卷积核边长
  kernels: 16,   // 3D 演示的卷积核个数（weights.js 提供 16 个真实核）
  outCap: 8,    // 输出通道数 = 真实模型的 8 个输出通道（3D 输出线框盒容量层数）
  V: 0.92,       // 体素留缝
  /* 3D 布局：各模块位置按尺寸自动排布（输入体 → 核排 → 输出盒），这里只调间距/方向/旋转 */
  layout: {
    origin: [0, 0, 0],    // 第 1 个卷积核中心 = 布局锚点
    gap: 5,               // 模块间净间距（输入体 ↔ 核排、核排 ↔ 输出盒）
    kernGap: 1.5,         // 相邻卷积核净间距
    kernDir: [1, 0, 0],   // 核排排列方向
    inputFace: [0, 0, 1], // 输入体深度轴（前面板朝向；+z = 朝向初始相机）
    flowRotZ: 0,          // 核排 + 输出盒整体相对输入体绕 Z 轴旋转（弧度；Math.PI/2 = 90°）
    camTheta: 0.32, camPhi: 1.02, // 初始视角（相机距离按场景跨度自动）
  },
};
